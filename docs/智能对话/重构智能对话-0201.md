协作式开发
多个实例可以同时访问同一个项目的会话历史
便于在不同终端/端口间切换工作
共享 Agent、模型配置等资源

- 新建 `chat_servers` 表 ,用于存储启动的opencode server 信息
- 新建 `proxy_host` 表,用于存储部署了 proxy 的服务器信息, 这些服务器可以用于通过 编写的 proxy程序启动opencode server

|对话服务列表 +| |
| 对话服务1 | |
| 对话服务1 | |
| 会话列表 +| |
| 会话1 | |
| 会话1 | |
| 会话1 | |
| AI Chat 输入框 |

- [x] `change:front` 删除智能对话->搜索会话 框
- [x] `change:front` 删除智能对话->底部的 新建对话 按钮
- [x] `feat:front` 重构"智能服务" 添加服务前后端

## 智能服务 添加服务前后端实现逻辑

点击添加按钮,弹出请需要创建的智能服务名称,最长16个字符,输入后点确认调用后端 nodejs api,后端实现如下逻辑

- 1. 数据库查询 SELECT id,host,port, begin_chat_port FROM proxy_host where status='active' order by updated_at asc limit 1 , 如果有返回数据条目则存储数据并进行下一步逻辑,如果没有则返回"无可用的代理服务,请添加代理服务"到前端
- 2. 获取 .env 中的 OPENCODE_BASE_PATH,PROXY_API_SECRET,OPENCODE_AUTH,OPENCODE_PASSWORD 变量值
- 3. 通过上一个sql 获取的id 值 查询 chat_servers 表: SELECT port FROM chat_servers where proxy_id={id} and status='active' order by created_at desc limit 1 ,如果返回的数据为空 则以 proxy_host 表中的begin_chat_port作为调用 proxy request port 参数, 否则 则以 chat_servers 返回的 port +1 作为 request 的port变量
- 4. 生成一个 由 用户名 + 随机16位字符串的 变量 与 OPENCODE_BASE_PATH 拼接为新的变量,假设变量定义为 ChatServerDir=OPENCODE_BASE_PATH+"/"+username+random6
- 5. 后端api 构造调用 proxy 服务的 http 请求:

---request
curl -X POST http://{proxy_host表返回的host}:{port}/api/opencode_start \
 -H "Content-Type: application/json" \
 -H "X-Signature: {PROXY_API_SECRET}" \
 -d '{
"port": {port},
"auth": {OPENCODE_AUTH},
"auth_password": {OPENCODE_PASSWORD},
"chat_dir": "{ChatServerDir}"
}'

---response  
{"code":200,"message":"服务启动成功"}

- 6. 调用proxy http api 服务 所有code 非200 的都是失败,失败将message 返回到前端
- 7. 如果返回code 为200 则将返回的port 等信息存储到 chat_servers 表中,并返回"服务启动成功"到前端

**备注**

- chat_servers 中存储的 name 为用户输入的智能服务名称
- error_message 列是另外一个后台程序定时检查opencode 服务如果有错误写入的, 当前新增不需要写入

## 前端布局调整

**任务目标：**
重构智能对话页面中现有的侧边栏（SessionSidebar）组件，将其布局从单一列表改为“上下分栏”结构。

**技术栈要求：**

- 组件库：shadcn/ui
- 样式库：Tailwind CSS V4
- 图标库：Lucide React

**具体布局与样式修改要求：**

1.  **整体容器布局 (Main Container):**
    - 将侧边栏的最外层容器修改为 Flex 纵向布局 (`flex flex-col h-full`)。
    - 确保侧边栏高度占满父容器，且宽度保持不变。

2.  **上半部分：智能服务:**
    - **位置：** 位于侧边栏顶部。
    - **布局行为：** 高度根据内容自适应（`flex-none`），或者设置一个最大高度限制（如 `max-h-[40%]`），防止占用过多空间。
    - **标题栏：**
      - 左侧显示标题文本 "智能服务"（搭配一个 Bot 图标）。
      - 右侧显示一个 "添加" 按钮（`+` icon），样式使用 `variant="ghost"` 的 icon button。
    - **内容区：** 用于展示服务列表的区域，以最多显示5行数据定义高度。

3.  **中部：分割线 (Separator):**
    - 在上半部分和下半部分之间，插入一个 shadcn/ui 的 `<Separator />` 组件。
    - 设置适当的上下 Margin (`my-2`) 以保持视觉呼吸感。

4.  **下半部分：对话列表区 (Chat List):**
    - **位置：** 位于分割线下方。
    - **布局行为：** 必须占据侧边栏剩余的所有垂直空间（关键样式：`flex-1 min-h-0`）。
    - **标题栏：**
      - 左侧显示标题文本 "对话列表"（搭配 Message 图标）。
      - 右侧显示一个 "新建对话" 按钮（`+` icon），样式同上。
5.  **视觉细节：**
    - 保持与当前设计一致的 Padding、字体大小和颜色规范。
    - 两个区域的标题栏样式应保持一致（字号、颜色、对齐方式）。

---

2026-02-03 09:55:00

- [x] `feat:front` 智能服务的服务列表定时通过异步轮询调用检查机制调用健康检查接口返回 服务健康状态并显示在对应服务名称的右边(以红绿灯表示),同时优化 智能服务列表的显示, 当前是显示服务名称+服务所在的 ip:port , 改为 服务名称 + 创建时间, 不再显示 ip和端口, 当前服务健康检查接口为: http://{服务ip}:{服务端口}/global/health, 返回的数据结构: {"healthy":true,"version":"1.1.48"},
      只有当 healthy 为 true 时服务为正常状态

- [x] `feat:front` 智能服务前端服务列表显示优化, 当用户点击服务列表某个服务时候,将该服务设置为当前激活的服务,同时将该服务显示样式高亮 - `add` 当前显示优化错误, 我需要的是点击服务列表某个服务他就是高亮状态,同时服务列表其他服务显示就是暗色状态,我需要在动作和视觉中体现用户当前激活的服务是哪个,因为后面服务是与对话关联切换的

- [x] `feat:all` 智能对话->对话列表基于以下逻辑重构:
  - 1.对话列表显示的是激活点击服务列表某个服务展现的对话列表数据,所以需要修改获取对话列表的调用请求 ip:port 改为选中哪个服务列表中的服务对应的ip:port
  - 2.对话列表中新建会话最多允许新建3个会话
  - 3.对话列表新建会话按钮只有在服务列表有激活服务的时候才显示
  - 4.如果未激活选中任何一个服务列表,则对话列表无任何对话显示
  - 5.对话列表随着用户激活服务列表某个服务,对话列表自动切换到该服务对应的对话列表,同时对话列表中的新建会话按钮也需要切换到该服务对应的对话列表
  - 6.对话列表的删除按钮参考 服务列表中的删除按钮实现,不再以 ... 而是一个删除图标点击删除

- [] `fix:backend` 当前智能对话->智能服务 删除某个服务并没有调用 proxy 服务的停止接口,导致后端服务一直在运行,请检查代码并修复,后端proxy 的停止接口为:
- 2. /api/opencode_stop 停止代理服务
  - 入参: { port": "int类型,对外服务的端口号"}
  - 出参: { "code": "int类型,状态码", "message": "string类型,提示信息"}

你可以参考/api/opencode_start 接口的实现方式, 注意: opencode_stop 接口需要通过nodejs 后端调用

- [x] `feat:backend` 智能对话->智能服务 当删除服务列表中的服务的时候,需要同时删除服务所加载的技能,mcp,agent信息,对应表为 chat_server_agents,chat_server_skills,chat_server_mcps 表,通过 chat_server_id 关联查询,然后删除

- [ ] `feat:all` 构建选中服务列表中的服务展现当前服务加载的 技能列表,mcp列表,agent列表信息, 在智能对话页面的最右侧,通过查询 chat_server_agents,chat_server_skills,chat_server_mcps 表 以 chat_server_id 关联 chat_servers表,同时过滤 chat_servers 表的 created_by 为当前登录用户id, 按创建时间排序

- [x] `fix:all` 智能对话->会话列表 中会话展现当前是通过调用 对应opencode 服务实例 /session 获取的会话, 现在需要修改为从数据库 sessions 表中获取, 过滤条件为: created_by + chat_id 其中 chat_id 为 chat_servers 表的 id 字段, created_by 为当前登录用户id,会话标题未 title字段展现, 按创建时间排序

- [x] `change:backend` 智能对话->智能服务列表 删除某个服务后端由原来调用`opencode_stop` 接口改为调用`opencode_delete` 接口, opencode_delete 接口需要额外的 char_dir 目录作为入参, 该参数通过查询 chat_servers 表的 chat_dir 字段获取

opencode_delete接口出入参信息:
/api/opencode_delete 停止代理服务并删除对应目录

- 入参: { port": "int类型,对外服务的端口号","chat_dir":"服务启动的目录"}
- 出参: { "code": "int类型,状态码", "message": "string类型,提示信息"}

- [x] `change:database` 修改数据库 sessions 表,删除 sessions_chat_id_fkey, sessions_created_by_fkey 外键关联,改为程序保证一致性,删除后更新 prisma schema
