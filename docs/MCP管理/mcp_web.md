

mcp管理页面 应该于技能箱页面布局样式类似, 但只是数据展现维度不一样,对于mcp来说它有几个核心点数据:

- 1. 顶部标签应该为 我的mcp, mcp市场 两个标签项
- 2. 右上角同样有创建按钮
- 3. 我的mcp标签项里面包含每个mcp 卡片标签, 卡片里面包含: mcp 名称, mcp描述,创建人,创建时间,mcp健康状态,mcp类型(本地还是远程)
- 4. 卡片点击更多显示: 卸载mcp, 查看详情
- 5. 点击查看详情除了包含 mcp 名称, mcp描述,创建人,创建时间,mcp健康状态,mcp类型 信息外还有 mcp装载的会话列表,以及mcp 工具列表,工具的名称,描述,接受的入参信息

- 6. mcp 市场展现的是当前系统可以调用的所有mcp 服务列表,跟我的mcp 有如下不同:

- 1. 卸载mcp 变为 删除mcp(如果用户有权限删除,没有权限则不展现此菜单), 查看详情不变
- 2. 新增装载mcp ,点击装载mcp 后 弹出弹框 选择装载到哪个会话上

- 3. 点击 创建mcp 弹出页面 需要用户填写如下内容: 