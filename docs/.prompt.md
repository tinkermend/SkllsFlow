我现在想基于当前项目 实现一个 ai chat 对话,但这个ai chat 对话有以下几个特征:

- 会话后端不是直接调用大模型,而是调用 opencode serve 启动的 http 服务
- opencode 能够提供哪些http api 可以阅读 https://opencode.ai/docs/server/ 获取
- 在ai 对话输入框 当输入 / 时,会通过 get 方法 调用 /command api 获取可用的command 列表展现给用户
- 用户可以在ai chat 页面左边创建新的会话
- 我后续有一个 skills 插件系统,mcp 系统,用户必须选择某个或某些skills后才可以开启ai chat对话,当当前不考虑这个
- 需要异步调用获取 event 输出并展现在ai chat 页面中
- opencode serve 自身实现了一个web 平台,实现了我上面列出想要实现的效果,如果你要参考可以访问 https://github.com/anomalyco/opencode 获取
- 不同的用户登录ai chat 调用的opencode serve都不同, 我会在用户登录的时候启动针对这个用户的 opencode serve,前端需要调用后端的api获取启动的 opencode serve 的 地址和端口信息然后在 http api 调用的时候访问的时这个用户的 opencode serve 地址和端口

- 当用户登出的时候清理 client 实例 + 通知后端停止对应的 opencode serve

我需要你帮助我理清以下事情:

- 1.这个ai chat 页面布局样式可以复用哪些开源的成熟组件? ai chat 菜单页面 类似chatgpt 页面布局
- 2.在当前项目中实现 https://opencode.ai/docs/server/ 的 http api 需要哪些组件?
- 3.同时进行ai chat 对话另外异步接受事件流 技术复杂度实现难么?
- 4.我还需要一个后端服务用于实现一些与数据库交互的应用逻辑,以及rbac等权限控制,技术框架选择 nodejs 还是 python ,golang 合适?
- 5.其他未考虑到但需要注意的点


- ai chat 库: https://www.prompt-kit.com/

post 
http://localhost:4096/session/ses_40f0cfa6affefAxnSTF2s37zAb/message
{"agent":"sisyphus","model":{"modelID":"glm-4.7","providerID":"zai-coding-plan"},"messageID":"msg_bf63ab87e001ZvnitBBjor5kvg","parts":[{"id":"prt_bf63ab87f001CwwJu67xcw8TZs","type":"text","text":"测试"}]}

{"messageID":"60f0044d-0b7b-41a1-af53-fc78ac3d9db9","agent":"sisyphus","model":{"modelID":"glm-4.7","providerID":"zai-coding-plan"},"parts":[{"type":"text","id":"ec112484-b9b5-4376-b60b-977345590349","text":"1+1等于几"}]}


todo:

- 1.思考过程没有打印出来
- 2.每个用户在哪个目录中启动opencode 各不相同
- 3.会话一多将新建会话给淹没了,
- 4.需要新增删除会话,和更改会话名称功能
- 5./command 描述文字只显示前20个字符,剩下以...表示
- 6.后端api 尚未实现将 创建的opencode进行存储管理


------

## 技能管理 前后端开发技术方案

